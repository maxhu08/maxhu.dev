import { NextPage } from "next";
import { PageContainer } from "~/components/page-container";

const Page: NextPage = () => {
  return <PageContainer>tmux-zen (page wip)</PageContainer>;
};

export default Page;
